--!strict
-- ReplicatedStorage/Shared/Constants/Scoring
-- Centralized scoring rules

local Scoring = {
	TargetScore = 1000,
	FinishPoints = 200,
	FirstFinishBonus = 50, -- tweak later; set to 0 if you don't want it
}

return Scoring